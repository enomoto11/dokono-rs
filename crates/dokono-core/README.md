# dokono-core

Internal library used by [`dokono-rs`](../dokono-rs) and its sibling tools.

**This crate is an implementation detail of the `dokono-*` tool family. API may break at any release. If you want the CLI, install `dokono-rs`.**
